# Use eza and display icons, requires Nerdfonts
alias ls="eza --icons -a --group-directories-first"

# use bat in simple mode, only syntax highlighting
alias cat="bat --style=plain --paging=never"

# ripgrep
alias grep=rg

# glow, markdown preview
alias md=glow

# helix
alias vi=hx

# zoxide

